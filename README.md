The project repository for EM learning with stein discrepancies.


# Requirements 
* pytorch == 1.6.0
* numpy 
* scipy

# Developments 
Install the package in editable mode:  `pip install -e score-EM`. 